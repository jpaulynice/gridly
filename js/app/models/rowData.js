/**
 * Modular Grid Row  definition.
 */
define(['jquery', 
        'underscore', 
        'backbone'], 
        function($, _, Backbone) {

    /**
     * Represents a single row of data in a grid.
     */
    return Backbone.Model.extend({});
});
