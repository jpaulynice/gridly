<thead>
<tr>
	{{#each columns}}
      <th>
			{{#if this}}
      			{{this}}
				<i data-sortdir="desc" data-sortby="{{this}}" class="icon-sort fa fa-sort-asc"></i>
				<i data-sortdir="asc" data-sortby="{{this}}" class="icon-sort fa fa-sort-desc"></i>
			{{/if}}
		</th>
	{{/each}}
</tr>
</thead>
<tbody></tbody>
<tfoot>
</tfoot>
